## 2024-05-23 - Form Accessibility Pattern
**Learning:** Adding explicit `htmlFor` and `id` attributes to form fields significantly improves accessibility by expanding the click target for inputs and providing screen readers with clear label associations.
**Action:** Always ensure that every form input has a unique `id` and a corresponding `label` with `htmlFor` attribute pointing to that `id`.
